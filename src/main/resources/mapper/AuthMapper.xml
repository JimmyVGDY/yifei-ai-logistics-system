<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jimmy.mapper.AuthMapper">

    <!-- 菜单结果映射给 Sa-Token 会话和前端侧边栏共用。 -->
    <resultMap id="MenuResultMap" type="jimmy.model.MenuVO">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="menu_name"/>
        <result property="path" column="menu_path"/>
        <result property="permissionCode" column="permission_code"/>
        <result property="sortNo" column="sort_no"/>
    </resultMap>

    <!-- 登录查询只取鉴权和会话需要的字段，角色信息通过 role_id 关联 sys_role。 -->
    <select id="findLoginUserByUsername" resultType="map">
        select u.id,
               u.user_code as userCode,
               u.username,
               u.real_name as realName,
               u.password,
               u.status,
               r.id as roleId,
               r.role_code as roleCode,
               r.role_name as roleName
        from sys_user u
        left join sys_role r on r.id = u.role_id
        where u.username = #{username}
    </select>

    <!-- 刷新当前会话时按登录 ID 重新读取用户和角色，保证角色调整后能尽快生效。 -->
    <select id="findLoginUserById" resultType="map">
        select u.id,
               u.user_code as userCode,
               u.username,
               u.real_name as realName,
               u.password,
               u.status,
               r.id as roleId,
               r.role_code as roleCode,
               r.role_name as roleName
        from sys_user u
        left join sys_role r on r.id = u.role_id
        where u.id = #{userId}
    </select>

    <!-- 兼容旧明文密码：首次登录成功后升级为 BCrypt 密文。 -->
    <update id="updatePassword">
        update sys_user
        set password = #{password}
        where id = #{userId}
    </update>

    <!-- 查询角色绑定的启用菜单，Service 层会把扁平结果组装成菜单树。 -->
    <select id="selectMenusByRoleId" resultMap="MenuResultMap">
        select m.id,
               m.parent_id,
               m.menu_name,
               m.menu_path,
               m.permission_code,
               m.sort_no
        from sys_menu m
        join sys_role_menu rm on rm.menu_id = m.id
        where rm.role_id = #{roleId}
          and m.status = 1
        order by m.sort_no, m.id
    </select>

    <select id="selectMenusByPermissionCodes" resultMap="MenuResultMap">
        select distinct m.id,
               m.parent_id,
               m.menu_name,
               m.menu_path,
               m.permission_code,
               m.sort_no
        from sys_menu m
        where m.status = 1
        <choose>
            <when test="permissionCodes != null and permissionCodes.size() > 0">
                and m.permission_code in
                <foreach collection="permissionCodes" item="permissionCode" open="(" separator="," close=")">
                    #{permissionCode}
                </foreach>
            </when>
            <otherwise>
                and 1 = 0
            </otherwise>
        </choose>
        order by m.sort_no, m.id
    </select>
</mapper>
