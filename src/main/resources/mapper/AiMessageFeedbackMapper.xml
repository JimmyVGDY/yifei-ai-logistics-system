<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jimmy.ai.mapper.AiMessageFeedbackMapper">

    <insert id="insert">
        insert into ai_message_feedback (id, message_id, conversation_id, user_id, rating, comment, created_at)
        values (#{id}, #{messageId}, #{conversationId}, #{userId}, #{rating}, #{comment}, #{createdAt})
    </insert>

</mapper>
