<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jimmy.logistics.mapper.LogisticsTrackMapper">

    <select id="countByOrderIdAndOperationDesc" resultType="int">
        select count(1)
        from logistics_track
        where order_id = #{orderId}
          and operation_desc = #{operationDesc}
          and deleted = 0
    </select>

    <insert id="insertTrack">
        insert into logistics_track (
            id, order_id, waybill_id, current_status, current_location,
            operator_name, operation_desc, operation_time
        )
        values (
            #{id}, #{orderId}, #{waybillId}, #{currentStatus}, #{currentLocation},
            #{operatorName}, #{operationDesc}, current_timestamp
        )
    </insert>
</mapper>
