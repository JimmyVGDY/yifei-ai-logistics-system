<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jimmy.logistics.mapper.LogisticsOrderMapper">
    <resultMap id="LogisticsOrderResultMap" type="jimmy.logistics.entity.LogisticsOrder">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="customer_name" property="customerName"/>
        <result column="sender_address" property="senderAddress"/>
        <result column="receiver_address" property="receiverAddress"/>
        <result column="cargo_name" property="cargoName"/>
        <result column="cargo_weight" property="cargoWeight"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findRecent" resultMap="LogisticsOrderResultMap">
        select id, order_no, customer_name, sender_address, receiver_address,
               cargo_name, cargo_weight, status, created_at, updated_at
        from logistics_order
        order by id desc
        limit #{limit}
    </select>

    <select id="findByOrderNo" resultMap="LogisticsOrderResultMap">
        select id, order_no, customer_name, sender_address, receiver_address,
               cargo_name, cargo_weight, status, created_at, updated_at
        from logistics_order
        where order_no = #{orderNo}
    </select>

    <insert id="insert" parameterType="jimmy.logistics.entity.LogisticsOrder" useGeneratedKeys="true" keyProperty="id">
        insert into logistics_order (
            order_no, customer_name, sender_address, receiver_address,
            cargo_name, cargo_weight, status, created_at, updated_at
        )
        values (
            #{orderNo}, #{customerName}, #{senderAddress}, #{receiverAddress},
            #{cargoName}, #{cargoWeight}, #{status}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="updateStatus">
        update logistics_order
        set status = #{status}, updated_at = current_timestamp
        where order_no = #{orderNo}
    </update>
</mapper>
